export default function ComingSoon({ role }) {
  return (
    <div className="flex min-h-screen items-center justify-center bg-canvas text-center">
      <div>
        <h1 className="text-xl font-semibold text-ink">{role} dashboard</h1>
        <p className="mt-2 text-sm text-subtle">Not built yet -- routing stub for a later round.</p>
      </div>
    </div>
  )
}
